import { BarChart3, Copy, LayoutGrid, Plus, Trash2, ChevronRight } from "lucide-react";
import { Form } from "../types";
import { Brand, Pill } from "./Shared";

export function Dashboard({ forms, create, open, remove, duplicate }: { forms: Form[]; create: () => void; open: (id: string) => void; remove: (id: string) => void; duplicate: (id: string) => void }) {
  return <div className="app-shell"><aside className="side"><Brand /><nav><button className="nav-item active"><LayoutGrid size={18} /> My workspace</button><button className="nav-item" disabled style={{opacity: 0.5}}><span style={{display:"flex",gap:"6px",alignItems:"center"}}>Team workspace <Pill>Soon</Pill></span></button></nav><div className="profile"><span className="avatar">AR</span><span><b>Aryan</b><small>Free workspace</small></span></div></aside><section className="dashboard"><header className="topbar"><div><p className="eyebrow">MY WORKSPACE</p><h1>Good evening, Aryan <span>✦</span></h1></div><button className="button primary" onClick={create}><Plus size={18} /> Create a form</button></header><div className="dashboard-tabs"><button className="selected">My forms <span>{forms.length}</span></button></div><div className="form-grid">{forms.map(form => <article className="form-card" key={form.id}><div className="card-top"><div style={{ display: "flex", gap: "10px", alignItems: "center" }}><div className="form-icon"><BarChart3 size={21} /></div><div className="card-actions"><button onClick={(e) => { e.stopPropagation(); duplicate(form.id); }} title="Duplicate"><Copy size={16} /></button><button onClick={(e) => { e.stopPropagation(); remove(form.id); }} title="Delete"><Trash2 size={16} /></button></div></div><Pill tone={form.status === "published" ? "green" : "orange"}>{form.status}</Pill></div><button className="card-main" onClick={() => open(form.id)}><h3>{form.title}</h3><p>{form.updatedAt}</p></button><footer><span><BarChart3 size={15} /> {form.submissions.length} responses</span><span>Open <ChevronRight size={15} /></span></footer></article>)}<button className="new-card" onClick={create}><span><Plus size={22} /></span><b>Start from scratch</b><small>Create a form your way</small></button></div></section></div>;
}
